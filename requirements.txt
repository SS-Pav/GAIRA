# GAIRA_Core runtime dependencies.
# Enough to run the inference engine and both Streamlit apps from a fresh clone.
# (A full frozen environment for the lab VM is in requirements_vm.txt.)

# ── numerical / scientific core ──
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
pyyaml>=6.0

# ── apps + figures ──
streamlit>=1.40
plotly>=5.20
matplotlib>=3.7

# ── optional (only to RE-RUN the representation benchmark; not needed for inference) ──
# torch>=2.0

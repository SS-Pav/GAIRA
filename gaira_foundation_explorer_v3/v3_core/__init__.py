"""GAIRA Foundation Explorer V3 — core (data + ui + pages).

Additive companion to gaira_foundation_explorer (V1) and gaira_foundation_explorer_v2 (V2),
both of which remain unchanged and runnable. Reads ONLY the committed representation-hierarchy
artifacts + the frozen atlas fingerprint (09ed804a…). Nothing retrained.
"""
